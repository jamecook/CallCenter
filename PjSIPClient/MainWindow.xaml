<Window x:Class="PjSIPClient.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="MainWindow" Height="750" Width="525" Closing="MainWindow_OnClosing">
    <Grid>
        <StackPanel>
            <Button  Margin="5" Command="{Binding InitCommand}">
                Init
            </Button>
            <Button  Margin="5" Command="{Binding CallCommand}">
                Call
            </Button>
            <Button  Margin="5" Command="{Binding TransferCommand}">
                Transfer
            </Button>
            <Button  Margin="5" Command="{Binding HangUpCommand}">
                HangUpAll
            </Button>
            <Button  Margin="5" Command="{Binding CallInfoCommand}">
                CallInfo
            </Button>
            <Button  Margin="5" Command="{Binding AnswerCommand}">
                Answer
            </Button>
            <TextBox Text="{Binding Messages}" VerticalScrollBarVisibility="Auto" TextWrapping="Wrap" Height="600"></TextBox>
        </StackPanel>
    </Grid>
</Window>
